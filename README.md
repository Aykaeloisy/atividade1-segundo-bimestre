# atividade1-segundo-bimestre
1-Ao executar o código, a mensagem "Olá Mundo" é exibida no console juntamente do tempo de execução de código.
2-Ao executar este código, a mensagem exibida no console fica com erros de ortografia, creio eu que a intenção era que a mensagem fosse exibida da seguinte forma:"Olá mundo, como vai?", mas em vez disso, a mensagem aparece no console como:"Olá MundoComo vai".
3-A diferença entre o resultado desse código para o código anterior é que o resultado do código anterior saiu errado ortograficamente e o código atual saiu mais certinho, o "\n" pula uma linha e acabou separando as frases de maneira mais agradável. Consta no console como:"Olá Mundo
                                                                              Como vai".
4-Quando este código foi executado, um espaço no console surgiu para que eu escrevesse um nome. Quando dei enter, o código leu a minha resposta e repetiu no console logo abaixo.
5-a)Quando este código foi executado, os números e adições matemáticas apenas apareceram no console sem nenhum cálculo ou resposta como se só tivesse copiado os números e operações que eu escrevi, a diferença é que apareceram um abaixo do outro por causa da quebra de linha.
b)As diferenças entre as linhas 6 e 7 é que a linha 6 contém o "\n" (quebra de linha), que pula para a linha seguinte antes do próximo código.
6-Ao executar o código, foi exibida no console a seguinte mensagem:"maior de idade", mas se por acaso o valor de idade fosse menor que 18, não será exibida nenhuma mensagem no console.
